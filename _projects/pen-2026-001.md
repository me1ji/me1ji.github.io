---
id: sec-2026-001
caseId: SEC-2026-001
title: "Kioptrix Level 1"
summary: "Using CVE-2003-0201 (Samba 2.2.1a buffer overflow) to gain root access."
tools: ["CVE-2003-0201", "DREAD", "MITRE ATT&CK", "Samba"]
---

![KioptrixL1](/assets/images/projects/kioptrixl1/kioptrixl1.png)

## Executive Summary
During the security assessment of the Kioptrix Level 1 machine, we identified a Critical vulnerability related to an outdated and unpatched file-sharing service (Samba 2.2.1a). 

If exploited, an attacker could gain complete, unrestricted control over the target system including access to all files, user accounts, and sensitive data without needing a username or password. 

The attack was carried out successfully in a controlled lab environment, confirming that the vulnerability is real and easily exploitable using freely available tools. 
The most critical takeaway is that running outdated software with no firewall protection can allow an outsider to take over an entire system in a matter of minutes. 


## Vulnerability Details

- Vulnerability Type: Stack-Based Buffer Overflow 
- CVE: CVE-2003-0201 
- Affected Component: Samba service (SMB port 139), specifically the trans2open request handler 

Description: Samba versions 2.2.x contain a critical buffer overflow in the way trans2 requests are processed. The flaw exists in code that runs before any authentication check, meaning no credentials are needed to trigger it. By sending a specially crafted trans2 packet, an attacker can overflow the stack buffer and overwrite the return address, redirecting execution to arbitrary shellcode. Metasploit's exploit/linux/samba/trans2open module automates this entirely.

## Business Risk and Impact

### Risk Assessment (DREAD Framework)

| Metric | Rating (1-10) | Notes |
| :----- | :------------------------: | :----: |
| **Damage**   | 10  | Attacker gains an instant root shell full system compromise, including all files, users, and sensitive data |
| **Reproducibility**   | 10 | The exploit is fully automated via Metasploit and succeeds every time with no variation |
| **Exploitability**   | 10 | Requires no credentials, no prior access, and no specialized knowledge just a few Metasploit commands |
| **Affected Users**   | 9 | Every user account, file, and service on the system is exposed once root is obtained |
| **Discoverability**   | 9 | Port 139 is immediately visible via a basic nmap scan; Samba version is trivially fingerprinted |
| **Total Score**   | **9.6 (Critical)** | Across all five metrics, this vulnerability scores near-maximum posing an extreme risk to any exposed system |


### CVSS Score
Vector String: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H 

Base Score: 9.8 (Critical)

| Metric Group | Metric | Value | Justification |
|:--------------|:--------:|:-----:|:---------------:|
| **Attack Vector (AV)** | Network | N | The exploit is launched remotely over the network; no physical access is required. |
| **Attack Complexity (AC)** | Low | L | No special conditions or preparation are needed; the exploit works reliably every time. |
| **Privileges Required (PR)** | None | N | The vulnerability is pre-authentication; zero credentials are needed. |
| **User Interaction (UI)** | None | N | The attacker does not need the victim to perform any action to trigger the exploit. |
| **Scope (S)** | Unchanged | U | The exploit impacts only the vulnerable Samba service and its host system. |
| **Confidentiality (C)** | High | H | Full root access exposes all files, password hashes, and sensitive data on the system. |
| **Integrity (I)** | High | H | The attacker can modify, delete, or plant any file on the system. |
| **Availability (A)** | High | H | The attacker can shut down, crash, or completely disable the target system at will. |


### MITRE ATT&CK Mapping
The attack followed a straightforward kill chain: the attacker discovered the target through network scanning, identified a critically outdated service, exploited a pre-authentication buffer overflow to land a root shell, and then secured persistence by manipulating credentials. No privilege escalation was needed at any point due to the service running as root.

| Tactic | Technique ID | Technique Name | How It Was Used |
|:--------|:--------------:|:----------------:|:-----------------:|
| Reconnaissance | T1046 | Network Service Discovery | `nmap` was used to scan the network and identify open ports and services on the target. |
| Reconnaissance | T1082 | System Information Discovery | `nmap -O` and `-sV` were used to fingerprint the target operating system and determine the Samba version. |
| Initial Access | T1190 | Exploit Public-Facing Application | The trans2open buffer overflow (CVE-2003-0201) was exploited to gain access through the exposed SMB port 139. |
| Execution | T1059.004 | Command and Scripting Interpreter: Unix Shell | A reverse shell (`/bin/sh`) was spawned on the target after successful exploitation. |
| Command & Control | T1571 | Non-Standard Port | A reverse TCP shell connected back to the attacker over a non-standard port. |
| Persistence | T1098 | Account Manipulation | The attacker ran `passwd root` to change the root password and maintain persistent access. |

## Proof of Concept (PoC)

### Phase 1: Reconnaissance

#### Host Discovery
Run nmap -sn to discover live hosts on the network. 

![poc1](/assets/images/projects/kioptrixl1/poc1.png)

There are 5 hosts the 2.1 is our network gateway and the 2.2 & 2.3 are the network interface cards and by process of elimination we now know that our target machine’s ip is 10.0.2.4


#### Service Fingerprinting
Once the target has been identified run nmap -O and nmap -sV to fingerprint the OS and open services. Port 139 (SMB) is found open

![poc2](/assets/images/projects/kioptrixl1/poc2.png)

Use msfconsole which launches the Metasploit Framework for vulnerability scanning and exploitation.

![poc4](/assets/images/projects/kioptrixl1/poc4.png)

We now know that our target is using samba 2.2.1a and running linux


### Phase 2: Initial Access

Go into metasploit and search for samba exploits at the very bottom we can see that an exploit named trans2open targets samba 2.2.x so lets use it

![poc8](/assets/images/projects/kioptrixl1/poc8.png)

use exploit/linux/samba/trans2open which selects the specific exploit module targeting a buffer overflow in Samba.

![poc5](/assets/images/projects/kioptrixl1/poc5.png)



### Phase 3: Execution & Post-Exploitation

![poc6](/assets/images/projects/kioptrixl1/poc6.png)

Command Breakdown:
 * Started reverse TCP handler on 10.0.2.15:4444 -> before metasploit sends the attack, it opens a "listening" port on your own machine.
 * Trying return address 0xbffff… -> the brute force buffer overflow attack
 * Command shell session 4 opened (10.0.2.15:4444 -> 10.0.2.4:1028) -> This shows the successful link

![poc10](/assets/images/projects/kioptrixl1/poc10.png)

We established that we now have root access to the machine using a reverse shell now lets change the root user password

![poc11](/assets/images/projects/kioptrixl1/poc11.png)
Tada

## Root Cause Analysis
The vulnerability exists because Samba 2.2.x fails to perform proper bounds checking on user-supplied data in the trans2 request handler before the buffer is written to the stack.
 * Primary Cause: No input validation or bounds checking on incoming SMB packet data.
 * Technical Breakdown: The trans2open function copies attacker-controlled data into a fixed-size stack buffer without verifying its length. This overwrites the return address and redirects execution to the attacker's shellcode. The exploit fires pre-authentication, so there is no login barrier whatsoever.
 * Compounding Factor: The Samba daemon was running as root (uid=0), so successful exploitation immediately yielded the highest possible privilege level with no further steps needed.

## Recommendations

Short-Term Fix (Patch)
 * Immediately upgrade Samba to version 2.2.8a or higher, or migrate to a currently supported release.
 * Block TCP/UDP ports 139 and 445 at the network perimeter firewall. SMB should never be exposed to untrusted networks.

Long-Term Prevention
 * Enforce the Principle of Least Privilege: run all services under dedicated low-privilege service accounts, never as root.
 * Disable null (unauthenticated) sessions by adding restrict anonymous = 2 to smb.conf.
 * Conduct regular vulnerability scans and patch audits to catch outdated software.
 * Implement host-based firewalls (e.g., iptables) as a secondary defense layer.
